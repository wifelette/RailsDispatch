<div id="page_additional">
  <h3>Edit Page</h3>
  <h4>You are editing a page, this will appear in the footer of the site.</h4>
</div>

<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %>

<%= render :partial => 'form' %>

<%= link_to admin_pages_path, :class => "simple_button" do %>
  <span>&larr; Back to all pages</span>
<% end %>
