<div id="page_additional">
  <h3>New Notification</h3>
  <h4>You are creating a new notification, this will appear at the top of the site during the range specified.</h4>
</div>

<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %>

<%= render :partial => 'form' %>

<%= link_to admin_notifications_path, :class => "simple_button" do %>
  <span>&larr; Back to all notifications</span>
<% end %>
