<div id="page_additional">
  <h3>Edit User</h3>
  <h4>Notifications are not sent automatically, you must notify the user of their credentials.</h4>
</div>

<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %>

<%= render :partial => 'form' %>
