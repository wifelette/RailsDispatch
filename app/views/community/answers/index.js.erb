$("div#questions").append("<%= escape_javascript(render :partial => 'question', :collection => @questions) %>");
$("div#pagination").replaceWith("<%= escape_javascript(pagination) %>");
$("#load_more").removeClass("loading");
$("#load_more span").html("Load more");
